import React from 'react';

export default function fontFeatureSettings() {

  return (<React.Fragment></React.Fragment>);
};

fontFeatureSettings.propTypes = {
};

fontFeatureSettings.propDefaults = {
};
